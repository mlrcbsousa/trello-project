Sprint details
<%= @sprint.name %>
<br>
<%= @sprint.man_hours %>-hours
<br>
<%= @sprint.start_date %>
<br>
<%= @sprint.end_date %>
<br>
<%= @sprint.total_days %>-days
<br>
ready made DATA to display
# statistics methods that can be called on a sprint instance
@sprint.contributors
@sprint.lists_count
@sprint.total_cards
@sprint.weighted_cards
@sprint.weighted_cards_count
@sprint.cards_per_size
@sprint.weighted_cards_per_size
@sprint.total_story_points
# only weighted cards, only contributing members
@sprint.cards_per_contributor
@sprint.story_points_per_contributor
@sprint.story_points_per_size
@sprint.progress
@sprint.story_points_progress
<br>
<div class="container">
  <%= pie_chart @sprint.cards_per_size %>
  <%= pie_chart @sprint.cards_per_member %>
  <div class="burndown">
    <h1>Current sprint burndown</h1>
    <%= render 'shared/burndown' %>
  </div>
  <div class="row">
    <div class="col-sm-6">
    </li>
    <h3>Team breakdown</h3>
    <div class="stat-card">
      <%= render 'shared/pie' %>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-6">
      <h3>Individual progress</h3>
      <div class="stat-card">
        <%= render 'shared/bar' %>
      </div>
    </div>
  </div>
</div>
</div>

